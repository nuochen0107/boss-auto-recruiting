# Boss 招聘流程自动化项目复盘与后续方案

> 2026-06-09 更新：当前 Dashboard 主流程已切换为“推荐页直接触达”，不再使用岗位画像、候选人评分或大模型 API。后文保留的是历史评审记录；其中评分相关内容不代表当前运行逻辑。旧 `boss-loop` 脚本仅作兼容保留，Dashboard 不会调用它。

## 1. 文档目的

本文用于整理最近两天实际运行 Boss 自动招聘项目后的流程认知、遇到的问题、业务需求变化，以及后续可以采取的工程优化方向。

当前项目已经验证了一个轻量级自动化闭环：

- 在 Boss 直聘推荐页、沟通页筛选候选人并自动触达。
- 自动收取候选人通过 Boss 沟通发来的附件简历。
- 将本地简历同步到飞书招聘 ATS 或飞书人才库。

目前飞书招聘相关权限尚未完全开启，因此完整 ATS 投递链路还没有完全执行，但已经通过飞书人才库上传验证了“Boss 简历 -> 本地文件 -> 飞书侧入库”的可行性。

## 2. 当前理想业务流程

当前招聘自动化的理想主流程如下：

1. 读取 Boss 沟通页、推荐页里的候选人信息。
2. 查看候选人主动打招呼内容、卡片信息、教育经历、工作经历等。
3. 提取关键词，并判断候选人与当前岗位是否匹配。
4. 不符合要求的候选人记录为 `screened` / `skip`，通常不自动打扰。
5. 符合要求的候选人发送求简历消息，并记录姓名、候选人 ID、岗位、发送时间。
6. 后续收简历阶段只搜索这些“已经求过简历”的候选人姓名。
7. 如果候选人发送了附件简历，则自动下载到本地。
8. 本地简历进入同步队列。
9. 再从本地同步上传到飞书招聘或飞书人才库。

## 3. 当前项目已经实现的能力

### 3.1 Boss 推荐页 / 沟通页触达

当前项目可以在 Boss 直聘上读取推荐页、沟通页里的候选人信息，并根据岗位画像、关键词、候选人简历摘要、聊天内容等做初步筛选。

对于符合要求的候选人，脚本可以自动发送求简历消息，并将候选人写入本地状态文件。

### 3.2 自动收取附件简历

项目可以读取本地状态中已触达、待收简历的候选人，在 Boss 沟通页搜索候选人会话，并尝试识别是否存在附件简历。

如果候选人已发送附件，脚本会打开附件预览，点击下载，将 PDF / Word 等简历文件保存到本地目录，并更新候选人状态。

### 3.3 飞书侧同步

项目可以扫描本地简历目录，结合候选人状态文件和同步队列，将简历上传到飞书侧。

当前飞书招聘 ATS 权限未完全开放，所以现阶段主要是上传到飞书人才库，证明链路可跑通。

## 4. 对应问题的解决思路

### 4.1 收简历阶段搜索框出现很多无关名字

问题判断：

收简历阶段搜索框里输入的候选人名字来自本地状态文件，不是脚本里写死的测试数据。当前字段范围设置偏宽，可能会把已处理过、异常状态、历史遗留、不同岗位的候选人也拿来搜索。

解决思路：

第一步应该收紧收简历目标池，让收简历阶段只处理真正处于“已求简历，等待候选人发送附件”状态的人。

建议规则：

- 必须是当前岗位的候选人，即 `job_name` 等于本次运行岗位。
- 必须有明确求简历记录，例如存在 `message_sent_at`。
- 优先要求 `decision` 为 `auto_contact`，避免把跳过或异常候选人带入收简历阶段。
- 状态只允许进入收简历队列：`attachment_requested`、`attachment_sent_by_candidate`、`attachment_received`、`download_failed`。
- 明确排除：`screened`、`eligible`、`resume_downloaded`、`ready_for_hire_sync`、`boss_completed`、`sync_queue_failed`、`paused_send_failed`。
- 对候选人姓名做基础校验，排除岗位名、时间、薪资、系统按钮文案、过长文本等误解析内容。

实现建议：

- 修改 `collect_visible_resumes.js` 中的 `getCollectTargets`。
- 增加 `shouldCollectCandidate(candidate, jobName)` 独立函数，方便测试和后续调整。
- 在运行开始时打印本轮收简历目标数，以及被排除候选人的原因统计。
- 增加一个 `--preview-targets` 或 `--dry-run` 输出，先展示将要搜索的人名列表，再执行真实搜索。
- 对历史状态文件做一次清理，把明显污染的数据标记为 `invalid_parsed_candidate` 或迁移到 archive。

决策参考：

这是当前最优先要修复的问题。它影响用户对系统可靠性的直观判断，也会增加 Boss 页面操作频率。修完后，收简历阶段会更像一个精准队列，而不是在历史状态池里翻旧账。

### 4.2 自动化流程被拆成多个手动指令

问题判断：

当前项目已经可以被 agent 触发，但仍然需要人工分阶段发指令。真实招聘场景中，候选人不会在刚打完招呼后立即发送简历，所以简单串行执行“打招呼 -> 收简历 -> 同步飞书”并不能真正解决流程自动化问题。

解决思路：

需要从“一次性串行脚本”升级为“状态驱动的轻量调度器”。调度器不应该每次都完整跑三步，而是根据时间、状态文件和同步队列判断当前该做什么。

建议工作流：

```text
低频触达候选人
  -> 写入已求简历状态
  -> 等待一段时间
  -> 周期性检查是否收到简历
  -> 有新简历则下载
  -> 有待同步文件则上传飞书
  -> 输出运行报告
  -> 异常时暂停并提示人工接管
```

实现建议：

- 新增一个本地调度入口，例如 `boss-auto-runner.mjs`。
- 支持 `--once`：执行一轮状态判断和必要动作。
- 支持 `--loop`：按配置间隔循环运行。
- 支持 `--dry-run`：只展示将执行哪些动作。
- 支持时间窗口配置，例如白天低频触达，晚间只收简历和同步。
- 打招呼、收简历、飞书同步仍然复用现有脚本，不在 runner 里重写页面逻辑。
- runner 根据状态文件判断是否需要执行某一步，而不是机械地每次都跑完整三步。

建议配置：

- `greet_enabled`
- `collect_enabled`
- `sync_enabled`
- `greet_interval_minutes`
- `collect_interval_minutes`
- `sync_interval_minutes`
- `max_greet_per_cycle`
- `max_collect_per_cycle`
- `quiet_hours`

决策参考：

这个方向能明显提升使用体验，但需要谨慎控制频率。建议先做“半自动循环”，而不是完全无人值守。验证码、登录过期、风控、页面异常仍然交给人工处理。

### 4.3 简历必须先下载到本地再上传飞书，流程较重

问题判断：

当前链路依赖本地文件中转：

```text
Boss 附件简历
  -> 本地 PDF / Word
  -> 本地状态文件 / 同步队列
  -> 飞书招聘 / 飞书人才库
```

这个方案便于排查和重试，但实际使用上比 Boss 自带飞书插件更重，也会占用本地磁盘并带来敏感文件管理问题。

解决思路：

短期不要立刻推翻本地中转链路，而是先把本地文件治理做好；中期再评估是否可以利用 Boss 自带飞书插件减少本地下载。

方案 A：继续本地中转，但轻量治理。

- 上传成功后自动归档或删除本地简历。
- 增加本地简历保留天数，例如 7 天、30 天。
- 状态文件只保留文件 hash、同步结果、飞书返回 ID。
- 增加清理脚本，定期清除已同步且过期的简历。
- 明确 `data/resumes` 是敏感目录，不应提交或外发。

方案 B：验证 Boss 飞书插件自动化。

- 观察 Boss 插件是否能稳定打开。
- 验证是否支持批量同步或单个候选人同步。
- 验证是否能选择目标人才库、职位或文件夹。
- 验证脚本是否能判断插件同步成功或失败。
- 如果插件流程稳定，再考虑替代本地上传。

方案 C：混合模式。

- 默认优先尝试 Boss 飞书插件。
- 插件失败、权限不足或无法确认结果时，回退到本地下载上传。

决策参考：

当前建议先采用方案 A，因为改动小、风险低，能快速改善本地文件占用和安全问题。Boss 飞书插件可以作为中期验证方向，不建议在当前 MVP 尚未稳定前直接切换主链路。

### 4.4 主动在推荐页筛选并联系候选人

问题判断：

除了处理主动打招呼的候选人，还希望系统能在 Boss 推荐页或类似广场场景中主动筛选候选人，并对合适的人进行主动沟通。

解决思路：

当前项目已经具备部分推荐页触达能力，后续应该把它产品化为一个可控的“主动获客/主动触达”模块，而不是无限刷推荐页。

建议流程：

```text
读取推荐页候选人卡片
  -> 提取姓名、学校、经历、技能、期望岗位、城市等信息
  -> 按岗位画像评分
  -> 低分候选人记录 skip
  -> 高分候选人进入待触达池
  -> 按额度和节流策略发送招呼
  -> 写入状态，避免重复触达
```

实现建议：

- 增加推荐页候选人评分报告，先 dry-run 展示候选人和评分，不直接发送。
- 每轮主动触达数量限制在较小范围，例如 3-5 人。
- 发送间隔随机化，避免短时间密集操作。
- 增加黑名单和去重机制。
- 对候选人信息不完整的情况降低自动发送概率。
- 对主动触达和候选人主动打招呼分开统计，便于评估效果。

需要明确的边界：

- 主动触达更容易触发风控，必须低频。
- 推荐页信息不如完整简历，误判概率更高。
- 不应为了自动化而扩大打扰范围。

决策参考：

可以做，但要放在收简历筛选和自动调度之后。推荐页主动触达是增长效率功能，前提是基础闭环稳定，否则会放大误搜、误触达和风控问题。

### 4.5 部署和运行门槛较高

问题判断：

当前方案依赖本地 Chrome 登录态、CDP Proxy、Node.js 脚本、本地状态文件、飞书开放平台配置、OpenClaw skill 和 Boss 页面结构。对于非技术用户来说，配置和排障成本较高。

解决思路：

短期不建议直接做服务化系统，而是先把本地部署做成可检查、可恢复、可交接的标准化流程。

实现建议：

- 增加 `RUNBOOK.md`，写清楚日常怎么启动、怎么暂停、怎么恢复。
- 增加 `.env.example`，把敏感配置从 YAML 中迁移出来。
- 增加 `scripts/self-check.mjs`，一键检查运行环境。
- 增加 `scripts/report-state.mjs`，查看当前候选人状态统计。
- 增加 `scripts/clean-stale-lock.mjs`，处理过期锁。
- 增加 `scripts/archive-completed-resumes.mjs`，处理已上传简历归档。

自检项建议包括：

- Chrome 是否打开。
- CDP Proxy 是否运行。
- Boss 页面是否登录。
- 当前岗位 `job_name` 是否配置。
- 状态文件路径是否存在且 JSON 可解析。
- 简历目录是否可写。
- 飞书 App ID / Secret 是否配置。
- 是否存在过期 lock。

决策参考：

当前阶段最适合做“本地工作流产品化”，不是做多人服务化平台。部署简化的目标不是让它完全无依赖，而是让依赖清晰、错误可见、恢复步骤明确。

### 4.6 Boss 风控问题

问题判断：

运行中出现过发送 2-4 条后触发验证码或风控的情况。初步看，刚开始频繁测试、多次重跑时更容易触发；稳定低频后可能会缓解。

解决思路：

风控问题不一定能完全消除，但可以通过节流、暂停、人工接管和运行窗口控制来降低触发概率。

实现建议：

- 每轮限制发送数量。
- 增加随机发送间隔。
- 避免短时间重复运行同一阶段。
- 避免在工作高峰期高频触达。
- 触发验证码、登录过期、安全验证时立即暂停，不硬重试。
- 在 run log 中记录触发原因和触发前操作数量。
- 将主动推荐页触达和被动收简历分开节流。

可配置项：

- `max_greet_per_cycle`
- `min_send_interval_seconds`
- `max_send_interval_seconds`
- `daily_greet_limit`
- `pause_on_captcha`
- `pause_on_quota_exhausted`
- `quiet_hours`

决策参考：

当前优先级低于流程合并、收简历筛选收紧、部署简化。原因是风控与平台策略强相关，短期很难通过代码彻底解决；更现实的做法是让系统低频运行、遇到风险及时暂停，并减少无效页面操作。

## 5. 需求整理

### 5.1 需求一：将分步骤 agent 指令合并为自动化流程

当前痛点：

用户需要反复告诉 agent 做不同阶段的事情，流程仍然依赖人工指令推进。

目标：

将招聘流程改造成一个可持续运行的自动化工作流，而不是一次性串行脚本。

建议形态：

```text
自动调度器
  -> 低频主动筛选 / 打招呼
  -> 定时检查待收简历候选人
  -> 下载新简历
  -> 同步飞书
  -> 输出日报 / 异常提醒
```

关键点：

- 打招呼和收简历不应该简单连续执行，而应该按时间窗口和候选人状态调度。
- 收简历应该是周期性检查，例如每 30-60 分钟检查一次。
- 飞书同步可以在有新简历时触发，也可以定时批量同步。
- 流程遇到验证码、登录失效、风控、下载失败时暂停并提示人工处理。

### 5.2 需求二：降低本地简历中转的复杂度

当前痛点：

Boss 上已有飞书插件能力，而当前自动化方案需要先下载到本地，再上传到飞书，流程较重。

可选方向：

#### 方案 A：继续本地文件中转，但做轻量化治理

做法：

- 简历下载到本地临时目录。
- 上传成功后自动归档或清理。
- 只保留必要状态和 hash。
- 本地简历保存周期可配置，例如 7 天或 30 天。
- 对敏感目录加说明和权限约束。

优点：

- 当前实现改动小。
- 可排错、可重试。
- 不依赖 Boss 插件内部能力。

缺点：

- 仍然占用本地空间。
- 仍然保存敏感文件。
- 流程体验不如原生插件。

#### 方案 B：优先利用 Boss 自带飞书插件

做法：

- 自动化不再下载简历。
- 在 Boss 页面上触发或辅助使用飞书插件。
- 尽量让简历留在 Boss / 飞书插件自身链路中。

优点：

- 更接近用户实际使用习惯。
- 不必维护本地简历仓库。
- 减少敏感文件落地。

缺点：

- Boss 插件页面结构和流程可能更复杂。
- 插件是否支持自动化批量操作需要验证。
- 出错时可观测性可能更差。
- 可能更容易受页面变动影响。

#### 方案 C：混合模式

做法：

- 默认优先走 Boss 飞书插件。
- 插件不可用、失败或权限不足时，回退到本地下载上传。

优点：

- 兼顾体验和兜底。
- 适合当前权限不完整的阶段。

缺点：

- 逻辑复杂度最高。
- 需要维护两套同步路径。

建议：

短期采用方案 A，先把当前链路跑稳；中期验证方案 B 是否可行；如果 Boss 插件自动化稳定，再考虑切到方案 C 或 B。

### 5.3 需求三：主动筛选推荐页候选人并打招呼

当前痛点：

仅处理主动打招呼的候选人不够，还希望自动化主动寻找合适候选人。

目标：

支持类似“首页 / 推荐页 / 广场”的候选人自动筛选：

- 读取候选人基础信息。
- 结合岗位画像进行评分。
- 合适则主动沟通。
- 不合适则跳过。
- 全部结果写入状态文件，避免重复触达。

关键风险：

- 主动触达更容易触发 Boss 风控。
- 推荐页候选人信息可能不完整，误判概率更高。
- 如果筛选逻辑偏宽，可能打扰不合适的人。

建议：

- 初期只做低频、小批量。
- 每轮限制主动触达数量，例如 3-5 人。
- 每次发送间隔随机化。
- 必须有去重和黑名单机制。
- 先 dry-run 输出候选人评分报告，再开启真实发送。

## 6. 后续解决思路

### 6.1 第一优先级：修正收简历候选人筛选范围

目标：

避免收简历阶段搜索大量无关名字。

建议改动：

- `collect_visible_resumes.js` 的 `getCollectTargets` 增加严格过滤。
- 只处理当前岗位。
- 只处理真正发过求简历消息的候选人。
- 排除已经完成飞书同步或 Boss 流程完成的人。
- 对历史污染状态做一次清理或迁移。

验收标准：

- 收简历阶段搜索框只出现近期已经求过简历的候选人。
- 不再搜索已经完成、已同步、旧岗位、异常解析的人。
- 日志中能清楚看到本轮收简历目标数量和筛选原因。

### 6.2 第二优先级：加入轻量调度器

目标：

把“打招呼、收简历、同步飞书”从人工一步步触发，变成一个半自动工作流。

建议做一个本地 runner，例如：

```text
boss-auto-runner.mjs
```

职责：

- 按配置执行不同阶段。
- 根据状态文件判断是否需要收简历。
- 根据同步队列判断是否需要上传飞书。
- 支持 `--once`、`--loop`、`--dry-run`。
- 支持时间间隔配置。
- 遇到异常时暂停并输出明确原因。

示例运行模式：

```text
上午：主动筛选和少量打招呼
每 30-60 分钟：检查待收简历候选人
有新简历：同步飞书
晚上：输出当日统计
```

注意：

不要把完整流程写成简单的：

```text
打招呼 -> 立刻收简历 -> 立刻同步飞书
```

因为候选人发送简历存在滞后，这种串行方式在真实场景中效果有限。

### 6.3 第三优先级：简化部署

目标：

让本地环境更容易安装、检查和恢复。

建议增加：

- `README.md` 或 `RUNBOOK.md`
- `.env.example`
- `scripts/self-check.mjs`
- `scripts/setup-local.mjs`
- `scripts/clean-stale-lock.mjs`
- `scripts/report-state.mjs`

自检应覆盖：

- Chrome 是否已打开。
- CDP Proxy 是否运行。
- Boss 是否登录。
- 配置文件是否存在。
- `job_name` 是否设置。
- 飞书密钥是否配置。
- 简历目录和状态目录是否可写。
- 是否存在过期 lock。

### 6.4 第四优先级：优化本地简历治理

目标：

降低本地简历中转的安全和存储负担。

建议：

- 增加上传成功后自动归档或删除选项。
- 增加本地简历保留天数配置。
- 增加 `data/resumes` 清理脚本。
- 状态文件只保存必要 hash、文件名、同步结果。
- 明确敏感数据目录不要提交、不要外发。

### 6.5 第五优先级：验证 Boss 飞书插件自动化可行性

目标：

评估是否可以绕过本地下载上传流程，直接利用 Boss 飞书插件完成简历管理。

需要验证：

- Boss 飞书插件入口是否稳定。
- 插件是否支持批量操作。
- 插件内是否能选择目标人才库或职位。
- 操作是否能通过 CDP 稳定完成。
- 是否比本地上传更少触发风控。
- 是否有明确成功/失败反馈。

决策：

如果插件自动化稳定，可以逐步替代本地上传；如果不稳定，仍保留当前本地中转作为主流程。

## 7. 建议的近期路线图

### 阶段 1：把当前 MVP 跑稳

目标：

先让现有流程不乱、不重复、不误搜。

工作项：

- 收紧收简历目标筛选。
- 清理历史污染状态。
- 增加状态统计和运行报告。
- 明确每次运行处理了哪些候选人。

### 阶段 2：做半自动循环

目标：

减少用户每一步都要给 agent 下指令的负担。

工作项：

- 增加本地调度 runner。
- 支持定时收简历。
- 支持有新简历时自动同步。
- 支持异常暂停。

### 阶段 3：优化上传和存储

目标：

降低本地简历文件负担。

工作项：

- 上传成功后自动归档或删除。
- 增加保留周期。
- 评估 Boss 飞书插件自动化。

### 阶段 4：扩展主动推荐页筛选

目标：

让系统可以主动发现合适候选人。

工作项：

- 推荐页候选人评分报告。
- 小批量主动触达。
- 风控节流。
- 黑名单和去重。

## 8. 关键决策参考

### 8.1 是否继续使用本地脚本方案？

建议：

短期继续使用。

原因：

- 当前已经跑通。
- 成本低。
- 易调试。
- 适合单人、小规模岗位招聘。
- 权限不完整时仍能用人才库跑通链路。

不建议现在立刻服务化，因为：

- Boss 登录态和验证码仍需要人工介入。
- 页面结构不稳定。
- 当前还没有多人协作、高并发、强审计需求。

### 8.2 是否必须接入飞书招聘 ATS？

建议：

不是当前阶段必须。

如果公司不方便开放飞书招聘管理权限，可以先继续上传到飞书人才库，作为简历沉淀和链路验证。

后续如果需要和正式招聘流程打通，再申请：

- 职位 ID。
- 简历来源 ID。
- 投递创建权限。
- 人才库 / 文件夹权限。
- 应用密钥和回调配置。

### 8.3 是否要绕过本地简历，直接使用 Boss 飞书插件？

建议：

先验证，不要立刻替换。

判断标准：

- 插件操作是否稳定。
- 是否支持目标人才库 / 职位。
- 自动化是否能确认成功结果。
- 失败时是否容易恢复。
- 是否真的比本地中转更省事。

如果插件稳定，再考虑把它作为默认同步方式。

### 8.4 是否要做全自动无人值守？

建议：

暂时不要做完全无人值守，而是做“低频自动 + 异常人工接管”。

原因：

- Boss 有验证码、登录态、风控。
- 候选人沟通涉及真实用户体验。
- 错误触达会产生业务风险。
- 简历属于敏感数据。

更合适的目标是：

```text
自动完成重复操作
人工处理验证码、异常候选人、权限问题、最终决策
```

## 9. 最小可行下一步

建议下一步先做三件事：

1. 修复收简历阶段目标筛选，避免无关名字进入搜索框。
2. 增加一个轻量 runner，把打招呼、收简历、同步飞书变成可配置的半自动循环。
3. 增加运行报告，告诉用户本轮筛选了多少人、触达了多少人、待收多少份简历、下载多少份、同步多少份、哪里失败。

完成这三件事后，项目就会从“几个能跑的脚本”升级为“一个可用的本地招聘自动化工作流”。
