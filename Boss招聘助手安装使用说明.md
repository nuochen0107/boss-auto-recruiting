# 如何安装、配置和使用「Boss 招聘助手」

## 一、产品简介

「Boss 招聘助手」是本地 macOS 招聘自动化工具，适用于 HR 在 Boss 直聘招聘端进行日常候选人处理。

工具由本地 App 和浏览器控制面板组成。HR 打开 App 后，会在浏览器中进入本地控制面板，通过面板完成岗位配置、环境检查、Boss 沟通页打招呼、推荐牛人页打招呼、收取简历、本地简历归档和飞书招聘同步。

当前已支持：

- 按岗位处理 Boss 沟通页候选人。
- 向符合条件的候选人发送索取附件简历话术。
- 按岗位切换 Boss 推荐牛人页并打招呼。
- 收取 Boss 附件简历，并按岗位保存到本地。
- 将本地简历按岗位同步到飞书招聘。
- 在控制面板中新增、停用和维护岗位配置。
- 在控制面板中退出本地助手服务。

> 截图占位：产品控制面板首页

## 测试版本说明

当前版本为内部测试交付版本，主要用于验证 Boss 招聘流程自动化、简历本地归档和飞书招聘同步效果。由于招聘平台页面、网络环境和账号状态可能存在差异，使用过程中如果遇到异常、卡住、识别不准或操作不符合预期的情况，欢迎及时反馈具体页面、岗位、候选人和操作步骤。我会根据反馈积极排查和修改，也请大家在测试阶段多多包容。

## 二、安装前准备

使用前请确认电脑满足以下条件：

1. 使用 macOS 系统。
2. 已安装 Google Chrome 浏览器。
3. Chrome 中可以正常登录 Boss 直聘招聘端。
4. 已收到「Boss招聘助手.app」或「Boss招聘助手-macOS-arm64.zip」安装包。

本工具已经内置 Node.js、简历解析组件和 OCR 组件。HR 电脑不需要单独安装 Node、Python、PyMuPDF、Tesseract 或 Xcode Command Line Tools。

## 三、安装 Boss 招聘助手

### 第一步：解压安装包

找到交付的压缩包：

```text
Boss招聘助手-macOS-arm64.zip
```

双击解压后，会得到：

```text
Boss招聘助手.app
```

> 截图占位：解压后的 App 文件

### 第二步：移动到应用程序

将「Boss招聘助手.app」拖到 macOS 的「应用程序」文件夹中。

> 截图占位：拖入应用程序

### 第三步：首次打开 App

双击打开「Boss招聘助手」。

如果 macOS 提示“无法打开，因为无法验证开发者”，请按以下方式处理：

1. 打开「系统设置」。
2. 进入「隐私与安全性」。
3. 找到关于「Boss招聘助手」的拦截提示。
4. 点击「仍要打开」。
5. 再次确认打开。

> 截图占位：隐私与安全性允许打开

### 第四步：进入控制面板

App 启动后，会自动打开浏览器页面：

```text
http://127.0.0.1:8787
```

这个页面就是「Boss 招聘控制面板」。

如果没有自动打开，可以手动在浏览器地址栏输入上面的地址。

> 截图占位：控制面板打开成功

## 四、配置 Chrome 和 Boss 页面

### 第一步：打开 Chrome 远程调试

打开 Google Chrome，在地址栏输入：

```text
chrome://inspect/#remote-debugging
```

确认页面中已经开启远程调试相关能力。

> 截图占位：Chrome 远程调试页面

### 第二步：登录 Boss 直聘招聘端

在同一个 Chrome 中打开 Boss 直聘招聘端，并完成登录。

建议至少打开以下页面之一：

- Boss 沟通页
- Boss 推荐牛人页

> 截图占位：Boss 招聘端登录状态

### 第三步：启动浏览器连接

回到「Boss 招聘控制面板」，在页面右侧「环境检查」区域点击：

```text
启动浏览器连接
```

启动后，再点击：

```text
立即检查
```

当 Chrome、控制服务、Boss 页面均显示正常后，就可以开始执行任务。

> 截图占位：环境检查通过

## 五、配置岗位

首次交付的 App 默认不内置岗位，HR 需要先在控制面板中新增岗位。

### 第一步：打开岗位配置

在控制面板中找到「岗位配置」，点击：

```text
编辑岗位配置
```

然后点击：

```text
新增岗位
```

> 截图占位：岗位配置入口

### 第二步：填写岗位标识

「岗位标识」是系统内部使用的稳定编号，建议使用英文、小写字母、数字和下划线。

填写示例：

```text
product_operations
new_media_operations
customer_manager
ai_app_intern
```

填写规则：

- 只能使用英文、数字、下划线或短横线。
- 不要填写中文。
- 同一个岗位标识不能重复。
- 保存并开始使用后，不建议随意修改。

> 截图占位：填写岗位标识

### 第三步：填写岗位名称

「岗位名称」是控制面板里展示给 HR 看的名称。

填写示例：

```text
产品运营
新媒体运营
客户经理
AI 应用实习生
```

> 截图占位：填写岗位名称

### 第四步：填写 Boss 岗位别名

「Boss 岗位别名」需要填写 Boss 页面实际显示的岗位名称。

如果 Boss 中同一个岗位可能显示不同写法，可以每行填写一个。

示例：

```text
AI Native 游戏开发实习生
AI 原生游戏开发实习生
```

注意：

- 这里的名称要尽量和 Boss 页面显示一致。
- 多个别名请每行填写一个。
- 系统会根据这些名称判断候选人属于哪个岗位。

> 截图占位：填写 Boss 岗位别名

### 第五步：填写飞书岗位 ID

「飞书岗位 ID」用于把本地简历同步到飞书招聘中的对应岗位。

如果暂时只使用 Boss 打招呼和收简历，可以先留空。留空时：

- 沟通页打招呼可以执行。
- 推荐页打招呼可以执行。
- 收简历可以执行。
- 飞书同步会跳过这个岗位。

如果需要同步飞书招聘，请填写对应的飞书招聘岗位 ID。

> 截图占位：填写飞书岗位 ID

### 第六步：保存岗位配置

确认岗位信息填写完成后，点击：

```text
保存配置
```

保存成功后，页面会提示「岗位配置已保存」，岗位下拉框和岗位路由会自动刷新。

岗位配置会保存在本机，不需要每次打开 App 都重新配置。

> 截图占位：保存岗位配置成功

## 六、执行方式说明

控制面板顶部有两种执行方式：

### 演练模式

演练模式用于检查页面识别和流程是否能跑通。

演练模式下：

- 不会真实发送消息。
- 不会真实下载简历。
- 不会上传飞书。
- 适合首次配置后测试环境。

> 截图占位：演练模式

### 真实执行

真实执行会实际操作 Boss 页面。

真实执行下：

- 会真实发送 Boss 消息。
- 会真实下载简历。
- 会真实写入本地归档。
- 如配置了飞书岗位 ID，会同步到飞书招聘。
- 会消耗 Boss 沟通权益。

执行真实任务前，请确认岗位、人数上限和 Boss 页面均无误。

> 截图占位：真实执行确认

## 七、使用日常招聘流程

日常招聘流程包含三个步骤：

1. 处理沟通页。
2. 收取简历。
3. 同步飞书招聘。

也可以直接使用「沟通并收简历」连续执行前两个步骤。

### 第一步：选择处理岗位

在「候选人处理链路」中选择「处理岗位」。

可选：

- 全部启用岗位
- 单个具体岗位

选择「全部启用岗位」时，系统会依次处理所有已启用岗位。

> 截图占位：选择处理岗位

### 第二步：设置人数上限

根据需要填写：

- 每个岗位最多沟通人数
- 本次最多收取简历人数
- 本次最多同步人数

首次测试建议设置较小数字，例如：

```text
每个岗位最多沟通人数：3
本次最多收取简历人数：3
本次最多同步人数：1
```

> 截图占位：人数上限设置

### 第三步：处理沟通页

点击：

```text
开始处理沟通页
```

系统会自动：

1. 打开 Boss 沟通页。
2. 按岗位切换 Boss 顶部岗位筛选。
3. 读取当前岗位候选人会话。
4. 判断是否已经发送过索简历话术。
5. 向未索要简历的候选人发送一次索简历消息。

> 截图占位：开始处理沟通页

### 第四步：收取简历

点击：

```text
开始收取简历
```

系统会自动：

1. 根据已发送索简历消息的候选人列表查找候选人。
2. 使用 Boss 全局搜索定位会话。
3. 校验候选人姓名和岗位。
4. 处理候选人发来的附件简历。
5. 打开简历预览并下载文件。
6. 将简历保存到本地岗位目录。
7. 如未发送过确认话术，则发送“简历已收到...”。

简历本地保存位置为：

```text
~/Library/Application Support/BossRecruiting/data/resumes/<岗位标识>/
```

> 截图占位：开始收取简历

### 第五步：同步飞书招聘

点击：

```text
开始同步飞书
```

系统会自动：

1. 读取本地已下载简历。
2. 根据岗位标识找到对应飞书岗位 ID。
3. 解析简历中的姓名、电话、邮箱、学校、经历等信息。
4. 创建或更新飞书招聘人才。
5. 将候选人投递到对应飞书招聘岗位。
6. 上传简历附件。

如果岗位没有填写飞书岗位 ID，系统会跳过该岗位的飞书同步。

首次同步建议「本次最多同步人数」设置为 1，确认无误后再逐步增加。

> 截图占位：开始同步飞书

### 第六步：沟通并收简历

如果希望一次执行「处理沟通页」和「收取简历」，可以点击：

```text
沟通并收简历
```

系统会先处理沟通页，再尝试收取已收到的附件简历。

> 截图占位：沟通并收简历

## 八、使用推荐牛人页打招呼

### 第一步：选择推荐岗位

在「推荐牛人页打招呼」区域选择「推荐岗位」。

该岗位来自前面保存的岗位配置。

> 截图占位：推荐岗位选择

### 第二步：设置推荐页任务参数

填写：

- 今日目标人数
- 每批人数
- 批次间隔

首次建议使用较小人数测试。

> 截图占位：推荐任务参数

### 第三步：开始推荐页任务

点击：

```text
开始推荐页任务
```

系统会自动：

1. 打开 Boss 推荐牛人页。
2. 切换到所选招聘岗位。
3. 读取推荐候选人。
4. 判断候选人是否适合打招呼。
5. 对符合条件的候选人发送打招呼消息。

> 截图占位：开始推荐页任务

### 第四步：查看今日报告

点击：

```text
查看今日报告
```

可以查看当天推荐页任务的执行情况。

> 截图占位：今日推荐页报告

## 九、暂停和退出

### 暂停日常招聘流程

如果日常招聘流程正在运行，可以点击：

```text
暂停当前流程
```

系统会向当前任务发送暂停请求。请等待页面状态更新，不要连续重复点击多个任务按钮。

### 暂停推荐页任务

如果推荐页任务正在运行，可以点击：

```text
暂停推荐页任务
```

### 退出助手

当天使用结束后，可以在「环境检查」区域点击：

```text
退出助手
```

退出助手只会关闭本地控制面板服务和浏览器连接服务，不会关闭 Chrome，也不会关闭 Boss 页面。

下次使用时，重新打开「Boss招聘助手.app」即可。

> 截图占位：退出助手

## 十、本地数据保存位置

App 的用户数据保存在：

```text
~/Library/Application Support/BossRecruiting
```

常用目录说明：

```text
config/jobs.json
```

保存岗位配置。

```text
config/feishu.env
```

保存飞书开放平台配置。

```text
data/resumes/
```

保存从 Boss 下载的简历文件。

```text
data/runs/
data/briefs/
```

保存任务运行状态、日志和同步队列。

一般情况下，HR 不需要手动修改这些文件。

## 十一、常见问题

### 1. 每次打开 App 都需要重新配置岗位吗？

不需要。

岗位配置保存后会持久化到本机：

```text
~/Library/Application Support/BossRecruiting/config/jobs.json
```

下次打开 App 会自动读取。

### 2. 岗位标识应该怎么填？

建议填写英文小写标识，例如：

```text
product_operations
new_media_operations
customer_manager
```

不要填写中文，不要频繁修改已经使用过的岗位标识。

### 3. 飞书岗位 ID 可以不填吗？

可以。

不填飞书岗位 ID 时，该岗位仍可以执行 Boss 沟通、推荐页打招呼和收简历，但同步飞书招聘时会跳过。

如果需要把简历自动上传到飞书招聘，则必须填写对应岗位的飞书招聘岗位 ID。

### 4. 手动上传飞书招聘和自动同步有什么区别？

手动上传时，HR 需要在飞书招聘后台进入对应岗位，点击新增候选人或导入简历，然后手动上传 Boss 下载的简历文件。

自动同步时，系统会读取本地简历，根据岗位配置中的飞书岗位 ID，自动创建或更新人才、创建投递记录并上传简历附件。

### 5. 环境检查提示浏览器连接服务未启动怎么办？

点击「启动浏览器连接」，等待几秒后再点击「立即检查」。

如果仍然失败，请确认：

1. Chrome 已打开。
2. Chrome 已登录 Boss 招聘端。
3. Chrome 远程调试页面已经打开并启用。
4. 没有同时打开多个旧的助手服务。

### 6. 环境检查提示没有发现 Boss 页面怎么办？

请在同一个 Chrome 中打开 Boss 直聘招聘端，并确认已经登录招聘端账号。

然后回到控制面板点击「立即检查」。

### 7. 真实执行前需要注意什么？

真实执行会实际操作 Boss 页面，并可能消耗 Boss 沟通权益。

建议先用演练模式检查环境，再切换到真实执行。

首次真实执行建议把人数上限设置小一些。

### 8. 为什么同步飞书时某个岗位被跳过？

通常是该岗位没有填写飞书岗位 ID。

请进入「岗位配置」，为该岗位填写飞书招聘岗位 ID 后保存，再重新执行同步飞书。

### 9. 简历下载到哪里了？

默认保存到：

```text
~/Library/Application Support/BossRecruiting/data/resumes/<岗位标识>/
```

每个岗位会有独立文件夹。

### 10. 点击退出助手后，为什么 Chrome 还在？

这是正常的。

「退出助手」只关闭本地助手服务，不关闭 Chrome，也不退出 Boss 登录状态。

### 11. 如果任务卡住了怎么办？

先不要重复启动多个任务。

建议按顺序处理：

1. 点击对应的暂停按钮。
2. 等待任务状态刷新。
3. 检查 Boss 页面是否有验证码、弹窗或登录失效。
4. 点击「立即检查」确认环境。
5. 再重新启动任务。

### 12. 可以同时运行推荐页任务和日常招聘流程吗？

不建议。

两个任务都会操作同一个 Boss 页面。请等待一个任务结束或暂停后，再启动另一个任务。

## 十二、推荐使用流程

首次使用建议按以下顺序操作：

1. 安装并打开「Boss招聘助手.app」。
2. 打开 Chrome 并登录 Boss 招聘端。
3. 在控制面板点击「启动浏览器连接」。
4. 点击「立即检查」，确认环境通过。
5. 点击「编辑岗位配置」。
6. 新增岗位并保存。
7. 先选择「演练模式」测试。
8. 选择具体岗位，设置较小人数上限。
9. 切换到「真实执行」。
10. 执行「开始处理沟通页」或「开始推荐页任务」。
11. 收到候选人附件简历后，执行「开始收取简历」。
12. 确认本地简历正常后，执行「开始同步飞书」。
13. 使用结束后点击「退出助手」。

> 截图占位：完整流程示意
